const mailSender = require('../../../utils/nodemailer');
const { newProducts } = require('../../../utils/templates');

const createAdminMails = () => {
  try {
  } catch (error) {}
};
module.exports = createAdminMails;
